@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Log Off", "Logout", "Login") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "Index", "Login") ]
    @:[ @Html.ActionLink("Register", "Index", "Register") ]
}
