@model IEnumerable<Calorie_Tracker.DAL.tbl_user_information>
           
@{
    ViewBag.Title = "User Information";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            tbl_user
        </th>
        <th>
            tbl_user_metric
        </th>
        <th>
            user_information_value
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_user.user_email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_user_metric.user_metric_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.user_information_value)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
